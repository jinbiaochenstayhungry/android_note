OpenGLES第二章总结

* 层的概念

	前面一章提到我们会渲染后帧，待渲染完成后，替换前帧显示在设备上，但是这个工作操作系统并没有完全开放给opengles来做，操作系统控制着最终的显示结果，因此引入了层的概念，渲染的画面会存在多个层，包括opengl程序渲染出来的层和系统渲染的层等等，最终操作系统会通过合成器，将多个层合成在一个帧缓存中，放入后帧缓存。

	这一操作iOS在Core Animation库中实现